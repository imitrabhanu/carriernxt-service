# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/carriernxt
spring.datasource.username=mitrabhanubal
spring.datasource.password=
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8081

# Security Configuration
spring.security.jwt.secret=THis-IS-a-VERY-long-SECRET-key-that-is-at-least-64-bytes-in-length-for-HMAC-SHA512-algorithm-12345678901234567890
spring.security.jwt.expiration=86400000

# CORS Configuration - Completely removing CORS properties
# These settings are now handled in WebConfig.java

logging.level.org.springframework.security=DEBUG
logging.level.com.carriernxt=DEBUG
